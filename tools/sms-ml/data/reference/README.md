# Seed Reference Samples

These files are recovered from local git history and are intended as seed data
for the ML workspace.

Files:

- `seed-train.json`
- `seed-val.json`
- `seed-summary.json`

Source:

- commit `cab99ec`
- historical paths `ml/data/processed/train.json` and `ml/data/processed/val.json`

Notes:

- the records appear synthetic or anonymized rather than raw personal SMS
- do not treat this dataset as a production-quality benchmark without further review
- use it as seed material for designing the new training and evaluation pipeline